const sql = require('mssql')
 
async () => {
    try {
        const pool = await sql.connect('mssql://wis:Grintsys2017@localhost/wis')
        const result = await sql.query`select * from USUARIOS`
        console.dir(result)
    } catch (err) {
        console.log(err);
    }
}